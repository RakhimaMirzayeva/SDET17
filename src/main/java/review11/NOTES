Set:
    Sets are special set of classes which do not allow duplicate elements.
    Set is the main interface and there are three main classes which implements this interface.
    1)HashSet 2)LinkedHashSet 3)TreeSet
HashSet: