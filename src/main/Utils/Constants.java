package main.Utils;

public class Constants {
    //whenever variables are static and final below is the convention we use with All UpperCASE with underscores
    public static final String CONFIG_FILE_PATH = System.getProperty("user.dir") + "\\Files\\Config.properties";

    public static final String EXCEL_FILE_PATH = System.getProperty("user.dir") + "\\Files\\Employees.xlsx";
}